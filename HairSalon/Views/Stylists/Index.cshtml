@{
  Layout = "_Layout";
}

@using HairSalon.Models;
@model List<HairSalon.Models.Stylist>;

<h1>Stylists</h1>

@if (@Model.Count == 0)
{
  <h3>No stylists have been added yet!</h3>
} 

  @foreach (Stylist stylist in Model)
  {
    <h4><li>@Html.ActionLink($"{stylist.StylistName}", "Details", new { id = stylist.StylistId })
  </li></h4>
  }
<br>
<div id="nav-links">
<p>@Html.ActionLink("Add new stylist", "Create") 🌺 @Html.ActionLink("Home", "Index", "Home")</p>
</div>
@* The third argument is optional and directs which controller to use for routing *@